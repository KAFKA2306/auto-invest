# 自動投資パフォーマンスダッシュボード

## プロジェクト概要

リアルタイムで投資パフォーマンスを監視・分析するためのダッシュボードアプリケーションです。AIを活用した投資戦略の生成と、継続的なパフォーマンス最適化を実現します。

### 主な機能
- マーケットデータの自動収集・分析
- AIを活用した投資戦略の生成
- リスク管理と自動取引の実行
- パフォーマンスの継続的最適化
- 重要な投資指標のリアルタイム表示
- レスポンシブデザイン
- アニメーション付きの数値更新

## システムアーキテクチャ

### レイヤー構成
1. データ収集レイヤー
2. 処理レイヤー
3. 分析レイヤー
4. 戦略レイヤー
5. 実行レイヤー
6. モニタリングレイヤー

### 技術スタック

#### フロントエンド
- React + TypeScript
- Tailwind CSS
- shadcn/ui コンポーネント
- Tanstack Query

#### バックエンド
- Python 3.9+
- FastAPI
- PostgreSQL
- Redis (キャッシュ)
- RabbitMQ (メッセージング)
- PyTorch, scikit-learn (機械学習)
- Docker, Kubernetes (インフラ)

## パフォーマンス指標

### 1. シャープレシオ (Sharpe Ratio)
- リスク調整後リターンの指標
- 1以上で良好なパフォーマンス
- 年率換算ベースで計算

### 2. 最大ドローダウン (Maximum Drawdown)
- 最大の下落率を示す指標
- 小さいほど良好
- リスク管理の重要指標

### 3. 勝率 (Win Rate)
- 利益が出たトレードの割合
- 50%以上が目標
- 戦略の安定性を評価

### 4. プロフィットファクター (Profit Factor)
- 総利益÷総損失
- 1以上で収益性あり
- 戦略の収益性を評価

## セキュリティ機能

- ロールベースアクセス制御
- 多要素認証
- セッション管理
- 監査ログ
- データ暗号化
- 整合性検証

## プロジェクトの構造

```
src/
├── components/
│   ├── PerformanceCard.tsx     # 指標表示カード
│   ├── PerformanceMetrics.tsx  # 指標グリッド
│   └── charts/                 # チャートコンポーネント
├── lib/
│   ├── calculateMetrics.ts     # 指標計算ロジック
│   ├── api/                    # APIクライアント
│   └── utils/                  # ユーティリティ関数
├── hooks/
│   └── useMetrics.ts          # カスタムフック
└── pages/
    └── Index.tsx              # メインダッシュボード
```

## セットアップ方法

1. リポジトリのクローン:
```bash
git clone <repository-url>
```

2. 依存関係のインストール:
```bash
npm install
```

3. 環境変数の設定:
```bash
cp .env.example .env
```

4. 開発サーバーの起動:
```bash
npm run dev
```

## 開発ガイドライン

### コンポーネント設計
- 単一責任の原則に従う
- Props型を明示的に定義
- 再利用可能なコンポーネントの作成
- エラーハンドリングの実装

### スタイリング
- Tailwind CSSクラスの活用
- レスポンシブデザインの確保
- アニメーションの適切な使用
- ダークモード対応

### パフォーマンス最適化
- メモ化の活用
- 適切なステート管理
- 効率的なレンダリング
- キャッシュ戦略の実装

### エラーハンドリング
- エラー境界の使用
- グレースフルデグラデーション
- ユーザーフレンドリーなエラーメッセージ
- リカバリーメカニズムの実装

## トラブルシューティング

### 一般的な問題と解決策
1. データ収集エラー
   - ネットワーク接続の確認
   - API制限の確認
   
2. 分析エラー
   - データ形式の検証
   - 計算ロジックの確認
   
3. 実行エラー
   - システムリソースの確認
   - 権限設定の確認

## 貢献方法

1. Issueの作成
2. ブランチの作成 (`feature/xxx` or `fix/xxx`)
3. 変更の実装
4. テストの実行
5. プルリクエストの作成

## API仕様

### REST API
- GET /api/v1/market - マーケットデータ取得
- POST /api/v1/analysis - 市場分析実行

### WebSocket API
- /ws/market - リアルタイムマーケットデータ

## ライセンス

このプロジェクトはMITライセンスの下で公開されています。

## 設定パラメータ

```yaml
system:
  max_connections: 1000
  timeout: 30
  retry_attempts: 3

analysis:
  window_size: 20
  confidence_threshold: 0.95
  max_lookback: 252

risk:
  max_position_size: 0.1
  stop_loss: 0.02
  max_drawdown: 0.15
```